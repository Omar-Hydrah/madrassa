var router = require("express").Router();

// All courses
router.get("/", ()=>{
	
});

// Display a single course's data
router.get("/:courseId", ()=>{
	
});

router.post("/:courseId/join-course", ()=>{
	
});


module.exports = router;